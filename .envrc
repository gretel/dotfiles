#!/usr/bin/env bash
#
# tom hensel <github@jitter.eu> 2016
# powered by https://github.com/gretel/automagic
#            https://github.com/zimbatm/direnv
# -*- mode: bash; coding:utf-8; tab-width: 4; indent-tabs-mode: nil; -*-

set -e

### use autobitch if available
if test -f "$(brew --prefix autobitch)/share/autobitch.sh"; then
		use_auto "$1"
		use_auto_python "$1"
		use_auto_ruby "$1"
fi

### add executables to PATH
test -d 'bin' && PATH_add 'bin'

export GITHUB_TOKEN="9a3d8bfe92f318e9fb2cfd83a05b664d93196166"
export HOMEBREW_GITHUB_API_TOKEN="4b483aaf9c77a4b4a79dd2689ebca6eb6e6dfe2e"
